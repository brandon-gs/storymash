/* 
  This array help to redirect in client if has not token and want to view someone of the next routes
*/
const publicRoutes = ["/login", "/register", "/", "/story/read/[id]", "/profile/[username]"]

export default publicRoutes
